import styles from "./Navbar.module.css";

const Navbar = () => {
	return (
		<div className={styles.nav}>
			<h1 className={styles.text}>React Hooks Tutorial</h1>
		</div>
	);
};

export default Navbar;
